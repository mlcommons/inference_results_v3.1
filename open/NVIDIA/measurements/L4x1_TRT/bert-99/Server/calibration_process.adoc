To calibrate this benchmark, first follow the setup steps in `open/NVIDIA/README.md`.

```
make calibrate RUN_ARGS="--benchmarks=bert --scenarios=Server"
```

For more details, please refer to `open/NVIDIA/README.md`.