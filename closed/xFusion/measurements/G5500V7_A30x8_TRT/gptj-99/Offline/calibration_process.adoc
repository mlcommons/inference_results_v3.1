To calibrate this benchmark, first follow the setup steps in `closed/xFusion/README.md`.

```
make calibrate RUN_ARGS="--benchmarks=gptj --scenarios=Offline"
```

For more details, please refer to `closed/xFusion/README.md`.